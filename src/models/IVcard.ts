export interface IVcard {
    redirectMessage: string;
    redirectCountdownTitle: string;
    redirectCountdownUnit: string;
    downloadPrompt: string;
    error: string;
    errorMessage: string;
}